# コピーして `.env` または `.env.local` にリネーム（ローカル専用なら `.env.local` 推奨）
# Vite は `VITE_` 接頭辞の変数だけクライアントに埋め込みます

# 省略可。未設定時は相対パス `/api`（`vite.config` の dev プロキシ → localhost:8080）
# 本番ビルドやプロキシを使わないときに絶対 URL を指定（末尾スラッシュ不要）
# VITE_API_BASE=https://api.example.com/api
